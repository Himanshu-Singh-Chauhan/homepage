<template>
  <h1 class="text-3xl font-bold underline">{{ msg }}</h1>

  <div class="card">
    <button type="button" @click="count++" class="border-red-500">count is {{ count }}</button>
  </div>
  
</template>


<script setup>
import { ref } from 'vue'

defineProps({
  msg: String,
})

const count = ref(0)
</script>


<style scoped>
</style>
